<div class="modal-body d-flex align-items-center justify-content-center vh-100">
    <div class="forgot-password-form border rounded p-4 shadow-sm w-50">
      <h5 class="text-center mb-4">Lấy lại mật khẩu</h5>
      <p class="text-center mb-4">Nhập email của bạn để nhận liên kết đặt lại mật khẩu.</p>
      <form action="http://localhost:3000/user/forgot-password-email" method="post" class="w-100">
        <div class="mb-3">
          <label for="forgot-password-email" class="form-label">Email của bạn</label>
          <input
            type="email"
            class="form-control"
            id="forgot-password-email"
            name="email"
            placeholder="Email của bạn"
            required
          />
        </div>
        {{#if error}}
          <div class="alert alert-danger" role="alert">{{error}}</div>
        {{/if}}
        <button type="submit" class="btn btn-primary w-100">Gửi yêu cầu</button>
      </form>
      <div class="text-center mt-4">
        <a href="/signin" class="text-decoration-none">Quay lại trang đăng nhập</a>
      </div>
    </div>
  </div>